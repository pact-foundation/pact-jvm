name: Windows Build

on:
  push:
    branches: [ master, v4.2.x ]
  pull_request:
    branches: [ master, v4.2.x ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.9
      uses: actions/setup-java@v1
      with:
        java-version: 1.9
    - name: Build with Gradle
      run: gradlew -s --no-daemon -i check
      shell: cmd
